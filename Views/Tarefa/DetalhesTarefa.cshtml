@model trilha_net_mvc_desafio.Models.Tarefa;

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhes da Tarefa </h1>
<hr/>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(t=> t.Titulo)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(t=> t.Titulo)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(t=> t.Descricao)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(t=> t.Descricao)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(t=> t.Status)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(t=> t.Status)
        </dt>
    </dl>
</div>

<a asp-action="EditaTarefa" asp-route-id="@Model.Id">Editar esta Tarefa</a>
<hr/>
<a asp-action="Index" class="btn btn-danger">Voltar pra lista de Tarefas</a>